import{_ as t,o as a,c as d,a2 as o}from"./chunks/framework.D5wUhsuj.js";const u=JSON.parse('{"title":"Composables","description":"","frontmatter":{},"headers":[],"relativePath":"composables/index.md","filePath":"composables/index.md","lastUpdated":1785946763000}'),s={name:"composables/index.md"};function i(c,e,n,r,l,p){return a(),d("div",null,[...e[0]||(e[0]=[o(`<h1 id="composables" tabindex="-1">Composables <a class="header-anchor" href="#composables" aria-label="Permalink to &quot;Composables&quot;">​</a></h1><p>Каталог: <code>src/composables/</code>.</p><p>Общие Vue-composables вне <code>features/</code> (инициализация приложения, клик снаружи, запись голоса).</p><hr><h2 id="useinitapp" tabindex="-1">useInitApp <a class="header-anchor" href="#useinitapp" aria-label="Permalink to &quot;useInitApp&quot;">​</a></h2><p><strong>Путь:</strong> <code>useInitApp/index.ts</code></p><p>Асинхронная инициализация виджета для <code>main.ts</code> и <code>entry.ts</code>.</p><p><strong>Делает:</strong></p><ol><li>Настраивает Axios (baseURL, credentials, chat auth interceptor).</li><li>Устанавливает mode в <code>settingsStore</code>.</li><li>Загружает данные проекта (<code>/api/init</code>) → цвета, startMessage, gallery, frozen.</li><li>Проставляет CSS-переменные бренда на target-элемент.</li><li>Поднимает auth-сессию (me / refresh / anonymous).</li><li>Загружает список чатов.</li></ol><p>При 404 init — <code>hideProject()</code> (виджет не показывается).</p><hr><h2 id="useclickoutside" tabindex="-1">useClickOutside <a class="header-anchor" href="#useclickoutside" aria-label="Permalink to &quot;useClickOutside&quot;">​</a></h2><p><strong>Путь:</strong> <code>useClickOutside/index.ts</code></p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">useClickOutside</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(targetRef, callback)</span></span></code></pre></div><p>Слушает <code>pointerdown</code>/<code>click</code> вне элемента (через <code>composedPath</code>). Cleanup на unmount.</p><p>Используется, например, в mobile edit-mode пункта истории сайдбара и в дропдаунах (<code>ProgressFilter</code>).</p><hr><h2 id="useaudiorecording" tabindex="-1">useAudioRecording <a class="header-anchor" href="#useaudiorecording" aria-label="Permalink to &quot;useAudioRecording&quot;">​</a></h2><p><strong>Путь:</strong> <code>useAudioRecording/index.ts</code></p><p>Запись голоса для поля ввода чата (<code>ChatAction</code>).</p><h3 id="два-режима-выбор-при-старте" tabindex="-1">Два режима (выбор при старте) <a class="header-anchor" href="#два-режима-выбор-при-старте" aria-label="Permalink to &quot;Два режима (выбор при старте)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Режим</th><th>Когда</th><th>Как получается текст</th></tr></thead><tbody><tr><td><code>speech-api</code></td><td>Есть <code>SpeechRecognition</code> / <code>webkitSpeechRecognition</code></td><td>Текст в браузере (<code>ru-RU</code>, continuous + interim)</td></tr><tr><td><code>media-recorder</code></td><td>Fallback</td><td>Blob → <code>UIUtils.webmToWav</code> → <code>chatStore.transcribeAudioMessage</code></td></tr></tbody></table><p>Параллельно всегда крутится визуализация <code>audioLevels</code> (Web Audio <code>AnalyserNode</code> + <code>requestAnimationFrame</code>, 28 столбиков).</p><h3 id="жизненныи-цикл" tabindex="-1">Жизненный цикл <a class="header-anchor" href="#жизненныи-цикл" aria-label="Permalink to &quot;Жизненный цикл&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>toggleRecording</span></span>
<span class="line"><span>  → getUserMedia</span></span>
<span class="line"><span>  → startSpeechRecognition | startMediaRecorder</span></span>
<span class="line"><span>  → таймер recordingTime + волны</span></span>
<span class="line"><span>  → stop</span></span>
<span class="line"><span>  → обработка текста (или skip)</span></span>
<span class="line"><span>  → finishProcessing (закрыть AudioContext, отпустить микрофон)</span></span></code></pre></div><h3 id="константы" tabindex="-1">Константы <a class="header-anchor" href="#константы" aria-label="Permalink to &quot;Константы&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Константа</th><th>Значение</th><th>Смысл</th></tr></thead><tbody><tr><td><code>MIN_RECORDING_TIME_SEC</code></td><td>1</td><td>Короче — abort/skip без ошибки «не удалось распознать»</td></tr><tr><td><code>MIN_AUDIO_BLOB_SIZE</code></td><td>1000</td><td>Fallback: слишком короткая запись</td></tr><tr><td><code>AUDIO_BAR_COUNT</code></td><td>28</td><td>Число столбиков UI</td></tr><tr><td><code>MIN_BAR_LEVEL</code></td><td>0.12</td><td>Минимальная высота столбика в покое</td></tr></tbody></table><h3 id="флаг-skipprocessing" tabindex="-1">Флаг <code>skipProcessing</code> <a class="header-anchor" href="#флаг-skipprocessing" aria-label="Permalink to &quot;Флаг \`skipProcessing\`&quot;">​</a></h3><p>Ставится при короткой записи, <code>abort</code>, <code>cleanupRecording</code> / unmount.<br> Тогда <code>onend</code> / <code>onstop</code> <strong>не</strong> вызывают <code>onTranscribed</code> и не пишут ошибку распознавания.</p><h3 id="публичныи-api" tabindex="-1">Публичный API <a class="header-anchor" href="#публичныи-api" aria-label="Permalink to &quot;Публичный API&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Поле / метод</th><th>Описание</th></tr></thead><tbody><tr><td><code>isRecording</code> / <code>isTranscribing</code></td><td>Состояния UI</td></tr><tr><td><code>recordingTime</code></td><td>Секунды записи</td></tr><tr><td><code>audioLevels</code></td><td><code>number[]</code> 0…1 для баров</td></tr><tr><td><code>toggleRecording</code></td><td>Старт/стоп с учётом <code>disabled</code> и фазы транскрибации</td></tr><tr><td><code>startRecording</code> / <code>stopRecording</code></td><td>Прямое управление</td></tr><tr><td><code>cleanupRecording</code></td><td>Принудительная отмена (unmount)</td></tr></tbody></table><h3 id="параметры" tabindex="-1">Параметры <a class="header-anchor" href="#параметры" aria-label="Permalink to &quot;Параметры&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">useAudioRecording</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(disabled, { onTranscribed?: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span></code></pre></div><ul><li><code>disabled</code> — computed/ref (например, открыта callback-форма).</li><li><code>onTranscribed</code> — куда отдать текст (обычно в textarea <code>ChatAction</code>).</li></ul><p>Ошибки пишутся в <code>chatStore.error</code> через <code>setError</code>.</p>`,35)])])}const b=t(s,[["render",i]]);export{u as __pageData,b as default};
